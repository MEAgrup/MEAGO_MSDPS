# MSDPS Build Plan & Progress Tracker

Cross-turn tracker so we never lose the thread. Source of truth: `MSDPS_FULL_PRD.md`.
Architecture: `MSDPS_STEP1_Architecture_Blueprint.md` (approved).

## Conventions for every module migration
Each module migration delivers, in order:
1. **Tables** (cols, types, FKs, defaults) + `status_changed_by/at` on lifecycle tables.
2. **ID** issuance trigger (validate mandatory → `next_code()` → set `code`, immutable).
3. **status_transitions** rows + attach `enforce_status_transition('<entity>')`.
4. **Derived fields** (generated col / trigger / view / pg_cron — per blueprint §4).
5. **RLS** policies (blueprint §5).
6. **Audit** trigger `capture_audit('<entity>')`.

## Progress

| Phase / Module | Entities | Status |
|---|---|---|
| **Phase 0 / Fase A — Foundation** | employees, id_sequences, status_transitions, audit_log, working_calendar, okr_targets + RLS helpers + Next.js shell (login, dashboard, Kelola Karyawan) + seed | ✅ **DONE & VERIFIED** on live `mvcckptntrvzujqaoxxh` (migr. 0001–0010). 19 staff seeded, auth login tested green, app builds clean. ⏳ only pending: user opens `npm run dev` for visual/role-gate check. |
| M3 — Campaign | `campaigns` | ✅ DB applied+tested (lifecycle) |
| M1 — Leads Database | `leads`, `prospect_attempts` | ✅ DB applied+tested (dedup, competitive claim Ajeng vs Galih, atomic win-resolution) |
| M2 — Marketing | `marketing_performance_records` + `v_marketing_metrics` | ✅ DB applied+tested (ROAS 4.38, CPL/CPRL/Quality) |
| M4 — Merchant Record | `merchants`, `merchant_platforms`, `services`, `service_catalog`, `package_catalog` | ✅ DB applied |
| M5 — Finance | `transactions`, `installments`, `creator_payouts` + `close_deal()` + `verify_payment()` | ✅ DB applied+tested (closing, routing gate, over-verify block). PYO milestone gating → Fase C. |
| **Fase B — UI** | Next.js screens + server actions for M1–M5 | ✅ **BUILD CLEAN**. Routes `/campaigns` (CRUD+lifecycle+ROAS dashboard dari v_marketing_metrics), `/leads` (register + import CSV + self-claim pool + papan kompetisi + dedup message), `/merchants` (detail services/trx + closing form → close_deal), `/finance` (antrian verifikasi → verify_payment + flag jatuh-tempo/bermasalah). Nav role-gated di (app)/layout. lib/format.ts helper. ⏳ pending: user cek visual via `npm run dev`. |
| M6 — Account & Service | `strategies`, `briefs`, `complaints` | 🗄️ **PENSIUN 2026-09-12 (migr. 0361)** — eksekusi pindah ke CDPS; DB utuh, UI = nisan. ✅ DB applied+tested (migr. 0200: assign_am, strategy approve/revisi, brief per divisi, komplain 2 pintu, execution_status state machine kedua di services, close_deal +p_package_id). |
| M7 — E-commerce | `sku_work_units`, `ecom_time_logs` | 🗄️ **PENSIUN 2026-09-12 (migr. 0361)** — eksekusi pindah ke CDPS; DB utuh, UI = nisan. ✅ DB applied+tested (migr. 0201: review 2-layer Lead→AM, timer 1-aktif/staff, completion% derived, cancel kurangi target). |
| M8 — Ads | `ad_campaign_records`, `weekly_performance_entries` | 🗄️ **PENSIUN 2026-09-12 (migr. 0361)** — eksekusi pindah ke CDPS; DB utuh, UI = nisan. ✅ DB applied+tested (migr. 0202: go-live approval Lead-only, budget per-currency IDR/USD, ROAS/CTR generated, Final Report gate sebelum Completed). |
| M9 — KOL | `creators`, `creator_bookings` | 🗄️ **PENSIUN 2026-09-12 (migr. 0361)** — eksekusi pindah ke CDPS; DB utuh, UI = nisan. ✅ DB applied+tested (migr. 0203: tanpa revision cycle, create_creator_payout PYO-Video/10 video & PYO-Live/5 jam lintas merchant → creator_payouts M5). |
| M10 — Live Stream | `live_stream_results`, `merchant_gmv_authoritative` | 🗄️ **PENSIUN 2026-09-12 (migr. 0361)** — eksekusi pindah ke CDPS; DB utuh, UI = nisan. ✅ DB applied+tested (migr. 0204: entry_status derived, v_ls_achievement, GMV otoritatif + confidence tag). |
| **Fase C — UI** | Next.js screens + server actions for M6–M10 | 🗄️ **PENSIUN 2026-09-12 (migr. 0361)** — eksekusi pindah ke CDPS; DB utuh, UI = nisan. ✅ **BUILD CLEAN + PREVIEW VERIFIED**. Routes `/account` (intake→assign AM, strategy, brief per divisi, komplain), `/ecommerce` (queue, work unit, checklist, timer, review 2-layer), `/ads` (ADC + go-live gate + WPE/Final Report), `/kol` (creator master, bookings, GMV, Payment Request milestone), `/livestream` (forward vendor, upload Template Baku, manual, resolve unmatched, achievement, GMV otoritatif), `/finance` +antrian disbursement PYO. Nav role-gated di (app)/layout. `npx tsc --noEmit` bersih; preview MCP: input manual LSR W2 → [Lengkap], achievement recompute 41%. |
| M12 — Task/SLA | `block_requests`, `v_speed_score` | 🗄️ **PENSIUN 2026-09-12 (migr. 0361)** — eksekusi pindah ke CDPS; DB utuh, UI = nisan. ✅ DB applied+tested (migr. 0206 + 0206b hotfix: block-request formal per Brief, approve≥lead→Brief [Blocked], auto-timer Fase C.2 = blocked-time exclusion inheren, resume stempel `blocked_to`; `v_speed_score` hari-kerja E-com 3hk/20SKU pro-rata·Ads 6hk go-live·KOL 5hk·LS start 3hk; staff tak bisa [Blocked] langsung). |
| M11 — Merchant Board | `v_merchant_board` (view) | 🗄️ **PENSIUN 2026-09-12 (migr. 0361)** — eksekusi pindah ke CDPS; DB utuh, UI = nisan. ✅ DB applied+tested (migr. 0207: 1 baris/Brief, kolom kanonik 6-state prioritas Blocked>In Review>In Execution>Planning>Intake>Completed, agregat unit per divisi, flag overdue/revisi≥3/blocked/pending-block, role-gate di WHERE). |
| M13 — Merchant Health | `merchant_health_snapshots`, `merchant_health_monthly` (pg_cron) | 🗄️ **PENSIUN 2026-09-12 (migr. 0361)** — eksekusi pindah ke CDPS; DB utuh, UI = nisan. ✅ DB applied+tested (migr. 0208: MHR-/MHRM- immutable, GMV 40% anti-double-count dari `merchant_gmv_authoritative` (fallback channel tertinggi [GMV Estimasi])·Completion 30%·SLA 30%·penalti komplain −5/−15/−30·sinyal revisi E-com/Ads; band 75/50; cron mingguan `msdps_health_weekly` Min 17:30 UTC + bulanan `msdps_health_monthly`; RPC manual OD/Director. `v_speed_score` di-split jadi `v_speed_score_internal` (definer, utk cron) + view publik ber-gate). |
| M14 — Team Performance | `performance_scores`, `v_okr_attainment` (pg_cron) | 🗄️ **PENSIUN 2026-09-12 (migr. 0361)** — eksekusi pindah ke CDPS; DB utuh, UI = nisan. ✅ DB applied+tested (migr. 0209: PERF- per staff/minggu, Output 40/Speed 30/Quality 30 ternormalisasi vs target role, komponen per role E-com/Ads/KOL(GMV-generating, bukan revisi)/AM(health portfolio+pickup review+komplain), bobot renorm atas komponen tersedia, staff tanpa aktivitas tak diberi baris; seed OKR 2026-Q3; `v_okr_attainment` kuartal-berjalan; cron `msdps_perf_weekly` Min 17:45 UTC). |
| M15 — Portals (internal) | `v_team_portal_*`, `v_management_dashboard` | 🗄️ **PENSIUN 2026-09-12 (migr. 0361)** — eksekusi pindah ke CDPS; DB utuh, UI = nisan. ✅ DB applied+tested (migr. 0210, Fase 1 internal: `v_team_portal_tasks`/`_performance`/`_blocks` + `v_management_dashboard` (Director/OD, semua merchant, `band_rank` sort At Risk dulu). Merchant Portal = Fase 2, TIDAK dibangun; CSAT di luar scope). |
| **Fase D — UI** | Next.js `/board` `/portal` `/management` + block-request | 🗄️ **PENSIUN 2026-09-12 (migr. 0361)** — eksekusi pindah ke CDPS; DB utuh, UI = nisan. ✅ **BUILD CLEAN + PREVIEW VERIFIED**. `/portal` (Team Portal: Tugas Saya + Skor Performa + Speed Score + Block Request + antrian approve SPV), `/board` (Kanban 6 kolom per merchant), `/management` (Director/OD: kesehatan merchant, ringkasan bulanan, OKR attainment, ranking performa + tombol "Hitung snapshot minggu ini"). `lib/actions/blocks.ts` (ajukan/putuskan/resume block + generate manual). Nav role-gated. `npx tsc --noEmit` bersih. Preview MCP: siklus block penuh via UI (ajukan BLK-0003→approve→Brief [Blocked]+timer stop→resume→blocked_to closed), Management Dashboard render lengkap, role-gate (Sepri redirect dari /management), console bersih. |
| **Fase E — MCN MEA** | `mcn_creators`, `upload_batches`, `creator_period_summary`, `creator_subcat_segment_gmv`, `creator_top_products`, `live_schedule_slots`, `brand_deals`, `deal_products`, `cooperating_shops`, `creator_requests`, `campaign_requests`, `acquisitions`, `referrals`, `special_projects`, `special_project_merchants`, `special_project_creators`, `v_project_summary`, `platform_alerts`, `app_config` + lib/mcn pure parsers/weeks/ingest/routing/indicators + Next.js routes + nav per team | ✅ **DB APPLIED + SMOKE TESTED (6/6) + BUILD CLEAN**. Migrasi 0300–0308 applied ke live 2026-07-15; smoke test 6/6 PASS: creator auto-ID, state machine, slot locking, shop_id uniq, referral constraint, project_summary accuracy. Harness rollback verified—DB bersih. Actions 9 file + routes 7 + nav per team selesai ter-commit; `npx tsc --noEmit` bersih; QC pure lib 37/37. ⏳ preview/manual test UI end-to-end pending. |
| **Fase E.1 — Creator Analysis** | ingest workbook "Creator Analysis" (username-key, 2 sheet Filter+Data), `lib/mcn/creator-analysis.ts` parser pure + `readWorkbookSheets`, UI `/mcn/creators` + `/mcn/workspace` (15 kolom, Detail Mingguan W1-W5 selector), platform_alerts binding_lost | ✅ **DB APPLIED (0309, 2026-07-16) + LIB QC 32/32 + BUILD CLEAN**. Format export TikTok nyata "Creator Analysis": workbook 2 sheet, 1 baris = 1 kreator/minggu; kolom Creator name/ID, Binding status, city, level, Sales value, Orders, AOV, Redemption amount+orders, New posts, Posts with sales, LIVE streams, Valid LIVE streams. Interview key: username = kunci identitas unik per platform; Sales value & Redemption keduanya disimpan (growth pakai Sales value/affiliate_gmv); sync master info-only, transisi status kreator tetap manual; niche & jenis_creator TIDAK auto-fill (menunggu export konten). Migrasi 0309: mcn_creators +username/city/creator_level/binding_status (unik per platform, lowercase), creator_period_summary +aov/redemption_amount/redeemed_orders/new_posts/posts_with_sales/live_streams/valid_live_streams; platform_alerts +binding_lost (create 'Previously bound', auto-resolve saat 'Bound'). Ingest router dua jalur: Creator Analysis→username-first; legacy→per-produk. UI 15 kolom Name|Username|CM|Status|Industry|Jenis|Level|Avg Pay GMV|Redeemed GMV|Komisi|Posts|Posts sales|Live|Valid live|Roster; rata-rata 3 bulan bulanan (null≠0). Form Industry dropdown (Dining/Accommodation/Things to Do), upload di /mcn/creators+/workspace, Detail Mingguan card. npx tsc bersih. Pending: end-to-end upload UI testing. |
| **Fase E.2 — Penutupan gap** | `setCreatorProfile` (`lib/actions/mcn-creators.ts`), `lib/mcn/industries.ts` | ✅ **BUILD CLEAN** (2026-07-16). UI manual "Jenis & Industry" ditambahkan ke card "Kelola Kreator" di `/meago/creators` (`ProfileRow`) — satu-satunya jalur pengisian `jenis_creator`/`niche` sejak fase export konten video **dibatalkan** (artefak dorman terkait dibiarkan di DB, tidak menunggu apa pun lagi). Kategori merchant final ditetapkan 3 industri TikTok GO (Dining/Accommodation/Things to Do), konsisten lintas modul via `INDUSTRIES`. |
| **Fase E.3 — Revisi QA /projects & workspace** | `special_projects` +`videos_needed`+`poi_location` (migr. 0310), `lib/mcn/project-status.ts` | ✅ **DB APPLIED (0310) + BUILD CLEAN** (2026-07-17). Form Buat Project Baru + list `/projects` dapat 2 field info OPSIONAL: **Total Video Dibutuhkan** (angka target, tanpa realisasi) & **Lokasi POI** (teks bebas) — tampil hanya di `/projects` (v_project_summary TIDAK diubah; UI baca langsung dari tabel). Card project di 3 workspace (CM/Acquisition/BizDev) kini menampilkan **semua project kecuali cancelled** + kolom Status berbadge: **[Persiapan]** bila start_date belum tiba (turunan tanggal via `projectBadge`, hari Asia/Jakarta — bukan status DB baru), [Berjalan]/[Draft]/[Selesai]. |
| **Fase E.4 — Status Kontrak + Edit terpadu Kreator** | `mcn_creators.status_kontrak` (migr. 0316), `updateCreator` (`lib/actions/mcn-creators.ts`), `EditCreatorModal` (`app/(app)/meago/creators/forms.tsx`) | ✅ **DB APPLIED (0316 staging) + BUILD CLEAN** (2026-07-23). Migrasi 0316: `mcn_creators` +`status_kontrak text` dengan **CHECK constraint** hanya 2 nilai valid: `'kontrak'` / `'non kontrak'` (enum via CHECK, konsisten pola kolom `status`/`jenis_creator`; nullable, baris lama tampil "—"). Tabel "Master Kreator" di `/meago/creators` dapat kolom **Kontrak** + kolom **Aksi**. Tombol **Edit** per baris membuka **modal/dialog** (`EditCreatorModal`) berisi form SEMUA kolom kreator (identitas/audit/komisi-sync read-only; kolom bisnis editable per tipe datanya): Nama (wajib), Username, Platform, Kota, Industry (dropdown INDUSTRIES), Jenis (dropdown), Level, Status (dropdown, dijaga state-machine DB), **Status Kontrak** (dropdown Kontrak/Non Kontrak), Binding Status, Owner CM, GMV/GMV Live/GMV Video/Ads Budget Cap (parseRupiah), Roster Live, Catatan, Top Niches (JSON) → server action `updateCreator` (validasi domain + feedback error inline, auto-close + refresh via `revalidatePath` saat sukses). `npx tsc --noEmit` + `next build` bersih. |
| **Target OKR — UI** | `/okr` (Director/OD set target per section) | ✅ **BUILD CLEAN + PREVIEW VERIFIED**. Target OKR TIDAK lagi hardcode — Director menetapkan per section (E-com/Ads/KOL/AM) per kuartal via `/okr`. Pure app code (tanpa DDL baru): tulis ke `okr_targets` lewat sesi authed (RLS `okr_manage`), pola supersede (nonaktifkan aktif lama → insert baru, histori tersimpan; unique index parsial). `lib/okr-metrics.ts` (katalog 6 metrik computable + default fallback + helper kuartal), `lib/actions/okr.ts` (`setOkrTarget`/`clearOkrTarget`), `app/(app)/okr/` (page+forms), switcher periode ±kuartal. Nav "Target OKR" mgmt-only + link dari card OKR di /management. `npx tsc --noEmit` bersih. Preview MCP (Yohan): ubah SKU E-com 20→25 → tersimpan, effective ↑, attainment /management ikut 25%→20% (engine baca live), supersede terverifikasi di DB (row 20 active=false, 25 active=true), "no change" guard, restore ke 20. Role-gate: Sepri redirect /okr→/dashboard, nav "Target OKR" absen. Console bersih. |
| **Bridge MSDPS→CDPS Fase 1** | `deal_bridge_lines`, `cdps_outbox` + `lib/bridge/payload.ts` + `lib/actions/bridge.ts` (`addBridgeLines`) + `app/api/internal/bridge/deliver` + `/deals` "Teruskan ke CDPS" | ✅ **DB APPLIED (migr. 0360) + BUILD CLEAN** (2026-09-11). Bukan modul PRD — otoritasnya `docs/DECISIONS.md` sisi `MEAgrup/AgencyAPP` (2026-09-10) + baris ini. Deal POI/merchant MEAGO yang **Berbayar DAN terverifikasi Finance** (`transaction_id` + `transactions.released_to_account_at`, gerbang trigger `deal_bridge_lines_gate` — `IS DISTINCT FROM`, bukan `<>`, karena `bentuk_kerjasama` nullable) bisa diteruskan sebagai satu order ke CDPS (`docs/BRIDGE_MSDPS_CONTRACT.md` payload v1, fixture dikomit identik di kedua repo). Satu order per deal seumur Fase 1 (`cdps_outbox_deal_id_uniq`). Delivery job = Next.js route dipicu scheduler eksternal (Vercel Cron `vercel.json`), **bukan** `pg_cron`/`pg_net` — lihat catatan penyimpangan di header migrasi 0360 (nol pemakaian pg_net di kedua repo, nol domain self-URL production yang terverifikasi untuk ditulis di migrasi). Backoff eksponensial, `dead` sesudah 5 percobaan + `platform_alerts` dead-letter (`cdps_bridge_dead`). Fase 1 satu arah — nol callback/`cdps_order_status` (Fase 2 belum dibangun). **`jenis` closed set naik ke ENAM nilai 2026-09-12 (migr. 0362, D2 dibalik):** + `Live Stream` — lihat baris Locked decisions. **Deprecation bertanggal:** kolom realisasi M6–M10 (Account/Ads/Creative/Store Operation manual di MSDPS) untuk deal yang di-bridge **deprecated, TIDAK di-drop** — dan sejak 2026-09-12 (migr. 0361) yang pensiun bukan lagi sekadar kolomnya untuk deal ter-bridge, melainkan **seluruh modul M6–M15**, lihat baris "Pensiun Account & Service" di bawah — eksekusinya sekarang di CDPS; MSDPS hanya mengirim & menandai `ord_code`. Tes: `scripts/test_bridge_gates.sql` (26 assertion sejak migr. 0362), `scripts/qc_bridge_payload.mjs` (50), `scripts/test_bridge_delivery.mjs` (16, retry/backoff/dead-letter mocked). |
| **Pensiun Account & Service** | nisan 8 route + nav + `lib/okr-metrics.ts` ditulis ulang + `okr_targets_meago`/`v_okr_attainment_meago` (migr. 0361) | ✅ **MIGRASI LOLOS DARI NOL (82) + BUILD BERSIH** (2026-09-12). Bukan modul PRD — otoritasnya baris ini. Konsekuensi langsung Bridge Fase 1: MEAGO tidak punya staff Account/Ads/Creative/Store Operation sendiri, jadi M6–M10 (+ turunannya M11–M15) adalah mesin eksekusi kedua yang tidak berpenghuni. **Pensiunnya LUNAK**: `/account /ecommerce /ads /kol /livestream /board /portal /management` diganti halaman nisan (`components/retired.tsx`, pesan `[modul ini sudah pindah ke CDPS — eksekusi layanan tidak lagi dijalankan di MEAGO]`, nol query DB), item nav dicabut, dan tiga pg_cron M13/M14 (`msdps_health_weekly`, `msdps_health_monthly`, `msdps_perf_weekly`) di-`cron.unschedule` — `msdps_retention_monthly` sengaja dibiarkan. **NOL `drop`/`delete`/`truncate`**: seluruh tabel, RLS, trigger, dan data historis M6–M15 tetap utuh dan terbaca OD/Director lewat SQL; satu `git revert` mengembalikan seluruh UI. `forms.tsx` + `lib/actions/{account,ecommerce,ads,kol,livestream,blocks}.ts` **sengaja dibiarkan yatim di disk** — begitu `page.tsx` berhenti meng-import-nya, Next.js tidak lagi mendaftarkan endpoint Server Action-nya (**diverifikasi**: nol referensi keenam modul itu di `.next/server/server-reference-manifest.json`, sementara okr/bridge/finance tetap terdaftar), jadi jalur tulisnya tertutup tanpa membuang jalan pulang. **TETAP HIDUP**: `/merchants` (M4) & `/campaigns` (M3) pindah ke grup nav "Merchant & Kampanye" — keduanya induk yang dipakai `close_deal` + bridge + Leads/ROAS, bukan modul eksekusi. **`/okr` selamat dan difokuskan ulang** ke divisi non-operasional (BizDev/CM/Acquisition/Marketing/Finance) dengan 7 metrik baru + attainment ditampilkan di `/okr` sendiri (pengganti `/management`). Tes: 82 migrasi lolos dari nol, `npx tsc --noEmit` bersih, `next build` bersih, bridge 50+16+25 assertion hijau, campaign-access 29, POI skor 8, content-analysis 55, campaign-completion 15. **✅ DI-APPLY KE LIVE `mvcckptntrvzujqaoxxh` 2026-09-12** (P2 dijawab: apply sekarang, bukan tunggu — bridge sudah terbukti mengalir untuk satu deal nyata, lihat bawah) bersama 0362 (D2 dibalik) + 0363 (P5+P7). Bukti integritas data sebelum/sesudah **identik** (briefs 10, strategies 3, complaints 2, sku 5, adc 2, bookings 13, lsr 3, health 11, perf 14). `cron.job` sesudah apply → hanya `msdps_retention_monthly` aktif. `information_schema.routine_privileges` mengonfirmasi `authenticated` tidak lagi punya EXECUTE atas ketiga generator (P5). `get_advisors(security)` sesudah apply: nol temuan baru — satu-satunya WARN baru (`v_okr_attainment_meago_rows` di daftar advisor 0029) sudah diantisipasi & dikomentari intensional di migrasi 0361 sendiri, pola yang sama dengan `v_okr_attainment_rows`/`v_merchant_board_rows` yang sudah lama ada. **Bukti bridge sudah mengalir sebelum apply ini**: `DEAL-202609-0078` → `ORD-202609-0001` → `CLI-202609-0017`, `[Diterima]` 2026-09-12 06:29 UTC — `external_service_map` (CDPS) sudah terisi 5 baris, jadi prasyarat go-live §6 lama sudah sebagian jalan; ini SATU deal dari target exit-criteria 5–10 (masih di bawahnya). |

## UI conventions (Fase B, learned)
- **JANGAN andalkan `name`/`value` tombol submit** untuk mengirim data ke Server Action (React 19/Next 15): nilai submitter TIDAK ikut ke FormData → action menerima field kosong. Untuk tombol transisi status, buat **satu `<form>` per aksi dengan hidden `<input name=... value=...>`**. (Bug ini sempat bikin advance prospek & lifecycle kampanye "ditolak: Data transisi tidak lengkap".) Diverifikasi via preview MCP: Ajeng advance Pending→…→Negotiation OK.
- **View agregasi lintas-RLS (`v_marketing_metrics`)** dibuat `security_invoker=true`, jadi saat dibaca sesi **Marketing** (yang tak punya akses RLS ke prospect_attempts/merchants/transactions) hasilnya 0 (ROAS/Revenue/Lead Real). Fix: campaigns/page.tsx membaca view via **klien service-role (`createAdminClient`, server-only)**, digerbang `is_od||is_director||division='Marketing'` (mirror RLS mpr). Diverifikasi: ROAS 29.97× muncul utk Bima. Alternatif DB-level (view SECURITY DEFINER + WHERE role-gate) bisa dipasang nanti bila akses DDL tersedia.
- **JANGAN `npm run build` saat `npm run dev` menyala** — build menimpa `.next` yang dipakai server dev → aset korup (CSS hilang/tampilan polos, JS klien mati/login gagal). Untuk typecheck pakai `npx tsc --noEmit`.
- **Campaign auto-activate**: create form punya checkbox "Langsung aktifkan" (default ON). Action insert [Draft] → update [Active] (via state machine, teraudit). Verified: CMP baru langsung [Active].
- **Merchant kategori = dropdown MEAGO**, daftar final (keputusan 2026-07-16): **Dining, Accommodation, Things to Do** — 3 industri TikTok GO, sumber tunggal `lib/mcn/industries.ts` (`INDUSTRIES`), dipakai juga di project `industry_category` dan Industry kreator (`niche`). Kolom DB tetap `text`; daftar di merchants/forms.tsx `KATEGORI`.

## Engineering conventions (enforced from Fase A)
- **Entity ID triggers MUST be `SECURITY DEFINER`** — `next_code()`/`next_code_global()` are revoked from anon/authenticated, so only owner-context (definer) triggers can mint IDs.
- Every lifecycle table carries `status_changed_by uuid`, `status_changed_at timestamptz` (stamped by `enforce_status_transition`).
- Attach order per table: ID/validation trigger (BEFORE INSERT/UPDATE) + `enforce_status_transition('<entity>')` (BEFORE UPDATE) + `capture_audit('<entity>')` (AFTER INSERT/UPDATE).
- Cross-module FKs added in the migration that creates the *referenced* table (avoids ordering failures).

## Locked decisions (do not re-litigate)
- State machines + ID + derived read-only enforced in **Postgres** (triggers/generated cols/cron).
- Audit log immutable for everyone incl. Director & service_role (trigger).
- OKR: per-role, quarterly, auto-achievement, owned by OD+Director.
- KOL booking SLA = **5 working days**. Login = email/password admin-invite.
- GMV authoritative = manual entry + confidence tag (API later).
- Merchant Portal = Fase 2 (NOT built now).
- **Pensiun M6–M15 bersifat LUNAK** (2026-09-12): UI dinisankan, DB utuh. Mengusulkan `DROP` tabel/kolom modul-modul itu = keputusan baru, bukan kelanjutan keputusan ini. Riwayat immutable adalah alasannya.
- **OKR MSDPS menilai divisi non-operasional saja** sejak 2026-09-12: BizDev, Creator Management, Akuisisi, Marketing, Keuangan. `okr_targets` + `perf_role` (M14) DIBEKUKAN — target baru ditulis ke `okr_targets_meago`.
- **D2 (bridge jenis) DIBALIK 2026-09-12** (migr. 0362): 'Live Stream' kini jenis bridge sah keenam — pekerjaan live-stream merchant MEAGO tetap diteruskan ke CDPS, bukan dikerjakan sendiri di MSDPS. Otoritas: instruksi pemilik sesi ini + amandemen `docs/DECISIONS.md` sisi AgencyAPP. `/livestream` TETAP nisan (migr. 0361) — konsisten dengan modul bridge lain, bukan dikeluarkan dari pensiun. Prasyarat operasional sebelum baris nyata bisa `accept()` di CDPS: paket "Live Stream" masuk Master Service List + `external_service_map` diisi (data, bukan migrasi — lihat §6 `docs/HANDOFF_PENSIUN_ACCOUNT_SERVICE_20260912.md`).
- **P5+P7 dijawab 2026-09-12** (migr. 0363): P5=ya — `generate_health_snapshots/_monthly`+`generate_performance_scores` ditutup dari `authenticated` (OD/Director tidak lagi bisa panggil manual; cron sudah mati sejak 0361). Reversibel: satu `grant execute ... to authenticated`. P7=ditandai inert — `okr_targets` lama + `v_okr_attainment` lama dapat `comment on` menunjuk ke `okr_targets_meago`/`v_okr_attainment_meago`; TETAP tanpa `DROP`.
