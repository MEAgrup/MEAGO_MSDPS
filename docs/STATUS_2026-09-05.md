# STATUS production ↔ staging ↔ repo — 2026-09-05

Hasil pemeriksaan langsung ke kedua database Supabase **hari ini**, bukan penyalinan
handoff sebelumnya. Semua angka di bawah diverifikasi dengan kueri, bukan asumsi.

- production `mvcckptntrvzujqaoxxh`
- staging `vgjzvdpxrdoefoncuazw`
- repo `main` = `2f1b17a` (migrasi terakhir `0352`, 73 file)

---

## 1. Jawaban singkat

**Skema production dan staging SUDAH SAMA PERSIS.** Tidak ada satu pun perbedaan
struktural yang tersisa. Satu-satunya yang berbeda adalah **daftar riwayat migrasi**
(catatan nama, bukan struktur) — item lama yang memang sengaja belum dikerjakan.

**Tapi ada temuan baru: repo TIDAK setara dengan keduanya.** Ada **6 objek yang hidup di
production dan staging tapi tidak bisa dihasilkan `db reset` dari repo.** Dua di antaranya
belum pernah tercatat di dokumen mana pun.

> **Diperbarui sore 2026-09-05 — T1 SUDAH DIKERJAKAN.** Migrasi
> `0353_auth_users_token_defaults.sql` sudah ditulis, diuji, dan diterapkan ke staging lalu
> production. Selisih repo ↔ live sekarang **5 objek**, bukan 6. Rinciannya di §8.

---

## 2. Paritas production ↔ staging — 11 dari 11 kategori identik

Metode: `scripts/schema_fingerprint.sql` dijalankan di kedua database, definisi fungsi
**dinormalisasi dulu** (komentar `--` dan whitespace dibuang, `→` disamakan dengan `->`)
supaya beda komentar tidak dilaporkan sebagai drift, lalu di-hash agregat per kategori.
Yang dibandingkan adalah **HASH**, bukan jumlah objek.

| Kategori | Jumlah | Hash agregat prod | Hash agregat staging | Sama? |
|---|---|---|---|---|
| kolom | 93 | `8b20fc05…` | `8b20fc05…` | ✅ |
| constraint | 72 | `8dbc3867…` | `8dbc3867…` | ✅ |
| index | 72 | `b494a612…` | `b494a612…` | ✅ |
| RLS policy | 70 | `02f8e29f…` | `02f8e29f…` | ✅ |
| RLS aktif | 72 | `c0ba2757…` | `c0ba2757…` | ✅ |
| fungsi | 106 | `171e1d52…` | `171e1d52…` | ✅ |
| trigger | 147 | `e2ce1bc5…` | `e2ce1bc5…` | ✅ |
| view | 21 | `4502b25e…` | `4502b25e…` | ✅ |
| enum | 34 | `d03450e0…` | `d03450e0…` | ✅ |
| bucket Storage | 3 | `3ecd3724…` | `3ecd3724…` | ✅ |
| job pg_cron | 4 | `14334755…` | `14334755…` | ✅ |
| **riwayat migrasi** | **81 vs 88** | `3e6487b4…` | `0b9e087d…` | ❌ |

Trigger di schema `auth` juga dicek terpisah (tidak tercakup sidik jari, yang hanya
melihat `public`): kedua environment sama-sama punya `auth.users.auth_users_token_defaults`
dengan definisi identik.

### Kenapa riwayat migrasi berbeda (dan kenapa itu bukan bug struktur)

Bukan karena ada migrasi yang belum jalan — strukturnya sudah dibuktikan identik di atas.
Penyebabnya tiga hal, semuanya soal **penamaan catatan**:

1. **Konvensi nama beda sejak awal.** Production mencatat `phase0_0001_extensions`,
   `faseB_02_module1_leads`, `poi_sop_tracking`; staging mencatat objek yang sama sebagai
   `0001_phase0_extensions`, `0101_module1_leads`, `0336_poi_sop_tracking`.
2. **Production kehilangan 2 baris riwayat** untuk migrasi yang objeknya jelas ada:
   `0312_creator_portal_f2` dan `0315_weekly_retention_dedup`. Sebaliknya ia mencatat
   `0336_ops_name_add_fifas` yang filenya tidak pernah ada (sudah digantikan `0350`).
3. **Staging menyimpan sampah riwayat** dari eksperimen SQL Editor yang objeknya sudah
   dibuang `0351`: `0317_acquisition_extra_fields`, `0318_acquisition_delete_policy`,
   `0319_acquisition_followups`, `0318_gmv_video_per_creator_week`, `0320_gmv_video_weekly`,
   `0317_gmv_video_weekly_tracking_fix`, `9001_drift_poi_brand_deals`,
   `tmp_vgmv_load_table`, `tmp_vgmv_raw_tables`, `cleanup_tmp_vgmv_load`, plus dua baris
   duplikat (`0338_lead_status_history`, `crm_leads_transaksi`).

Konsekuensi praktis: **daftar nama migrasi tidak boleh dipakai sebagai sumber kebenaran**
untuk menentukan environment mana yang tertinggal. Pakai sidik jari.

---

## 3. TEMUAN BARU — repo ↔ live TIDAK setara (6 objek)

Ini yang tidak ketahuan handoff sebelumnya, persis seperti yang diperingatkan jebakan §3
poin 2: *"kalau keduanya sama-sama menyimpang dari repo, diff antar-environment akan
terlihat bersih dan menipu."* Itulah yang terjadi.

Metode: `bash scripts/pg_test_reset.sh` (✅ 73 migrasi lolos dari nol) menghasilkan
database bersih dari repo, lalu sidik jari yang sama dijalankan di sana dan dibandingkan
ke production — bukan hanya staging ↔ production.

Sesudah **2 tabel + 4 fungsi** di bawah dikecualikan, ketujuh kategori yang bisa
dibandingkan **cocok persis** dengan production:

| Kategori | reset repo | production (dikecualikan) | Sama? |
|---|---|---|---|
| kolom | 91 / `b3240608…` | 91 / `b3240608…` | ✅ |
| constraint | 70 / `29b870f0…` | 70 / `29b870f0…` | ✅ |
| index | 70 / `13526077…` | 70 / `13526077…` | ✅ |
| policy | 68 / `b12f6605…` | 68 / `b12f6605…` | ✅ |
| RLS aktif | 70 / `b02ea506…` | 70 / `b02ea506…` | ✅ |
| fungsi | 102 / `71c7b622…` | 102 / `71c7b622…` | ✅ |
| trigger | 142 / `b3412d55…` | 142 / `b3412d55…` | ✅ |
| view | 21 / `4502b25e…` | 21 / `4502b25e…` | ✅ (tanpa pengecualian) |
| enum | 34 / `d03450e0…` | 34 / `d03450e0…` | ✅ (tanpa pengecualian) |

Jadi seluruh selisih repo ↔ live **persis** enam objek berikut, tidak lebih:

| # | Objek | Ada di | Ada file migrasi? | Status |
|---|---|---|---|---|
| a | tabel `crm_leads` + `crm_leads_validate()` | prod & staging | tidak (sengaja) | menunggu keputusan — item 4 |
| b | tabel `crm_transaksi` + `crm_transaksi_validate()` | prod & staging | tidak (sengaja) | menunggu keputusan — item 4 |
| c | `public.auth_users_token_defaults()` + trigger `auth.users.auth_users_token_defaults` | prod & staging | ~~TIDAK~~ → **`0353`** | ✅ **SELESAI** (§8) |
| d | `update_poi_realisasi(uuid,date,date,int,int,boolean)` | prod & staging | **TIDAK** | **BARU** |

(a dan b menyumbang selisih 2 tabel → 2 kolom, 2 constraint, 2 index, 2 policy, 2 RLS,
5 trigger, 2 fungsi. c dan d menyumbang 2 fungsi + 1 trigger di schema `auth`.)

### 3c. `auth_users_token_defaults` — paling berisiko, dan tidak butuh keputusan apa pun

Tercatat di riwayat kedua environment sebagai `0318_auth_users_token_null_guard`, tapi
`git log --all -- 'supabase/migrations/0318*'` **kosong** — filenya tidak pernah ada.
`grep -rn "auth_users_token_defaults"` di seluruh repo juga **nol hasil**.

Isinya trigger `BEFORE INSERT OR UPDATE ON auth.users` yang mengubah 8 kolom token GoTrue
dari NULL menjadi string kosong (`confirmation_token`, `recovery_token`,
`email_change_token_new`, `email_change_token_current`, `email_change`, `phone_change`,
`phone_change_token`, `reauthentication_token`). Ini penangkal bug GoTrue
*"converting NULL to string is unsupported"* yang muncul saat user dibuat lewat SQL/admin API.

**Kenapa ini penting sekarang:** jalur pembuatan akun Portal Kreator
(`createCreatorAccount` di `lib/actions/portal.ts`) memakai admin API. Di environment baru
hasil `db reset` — tanpa trigger ini — pembuatan akun berpotensi gagal dengan error yang
sama sekali tidak menunjuk ke penyebabnya. Dan itu tepat jalur yang harus dipakai untuk
membuka blocker item 2.

Perbaikannya tidak butuh keputusan bisnis: cukup resmikan apa adanya ke file migrasi
(idempoten, no-op di kedua environment yang sudah punya). **✅ Dikerjakan — lihat §8.**

### 3d. `update_poi_realisasi` — nasibnya terikat keputusan item 5

Fungsi ini juga tidak punya file migrasi. Handoff item 5 sudah membuktikan **tidak ada
satu baris kode pun yang memanggilnya**. Jangan diresmikan ke repo sebelum item 5 diputus:
kalau opsi **B** dipilih (sumber kebenaran pindah ke `poi_sop_progress`), fungsi ini justru
harus **dibuang**, bukan didokumentasikan.

---

## 4. Keadaan data (production, hari ini)

Tidak ada perubahan sejak audit 2026-09-04 — tidak ada aktivitas baru.

| Ukuran | production | staging |
|---|---|---|
| `mcn_creators` | **2.250** | 3.505 (2.248 lolos filter roster bersih) |
| kreator punya akun portal (`auth_user_id`) | **1** | — |
| `niche` terisi | **1** | — |
| `city` terisi | 33 | — |
| `creator_level` terisi | 32 | — |
| `brand_deals` | 81 | 0 |
| deal tanpa `kategori_poi` ("belum lengkap") | **70** | — |
| deal dengan `campaign_enabled` | **0** | 0 |
| `campaign_participants` | **0** | 0 |
| `campaign_video_submissions` / `_live_` | **0** / 0 | 0 |
| `campaign_curation_batches` | **0** | — |
| `campaign_payouts` | **0** | — |
| `campaign_budget_log` / `campaign_ads_spend` | 0 / 0 | — |
| `tiktok_post_index` | 0 | — |
| `deal_change_requests` | 0 | — |
| `creator_video_gmv` | 0 | 0 |
| `brand_deals.visit_checked = true` | **0** | — |
| `brand_deals.poin > 0` | **0** | — |
| `poi_sop_progress` (baris / `actual_vt` terisi) | 10 / **2** | — |
| `crm_leads` / `crm_transaksi` | 0 / 0 | **5 / 1** |
| `employees` | 35 | 31 |

Dua hal yang berubah artinya dibanding handoff:

1. **`crm_leads` & `crm_transaksi` tidak kosong di staging** (5 dan 1 baris). Handoff item 4
   hanya menyebut "0 baris di production" — benar, tapi rencana drop akan **menghapus data
   staging**. Perlu dipastikan itu memang baris uji sebelum drop dijalankan.
2. **Fase G masih 0 baris di seluruh tabel campaign.** Status jujurnya tetap
   "selesai dibangun & lolos unit test", **bukan** "teruji".

---

## 5. Keadaan kode (diverifikasi di sesi ini)

| Perintah | Hasil |
|---|---|
| `bash scripts/pg_test_reset.sh` | ✅ **74 migrasi lolos dari nol** (73 sebelum `0353`) |
| `npx tsc --noEmit` | ✅ bersih |
| `npm run build` | ✅ bersih |
| `node scripts/test_campaign_completion.mjs` | ✅ **15 lolos, 0 gagal** |

Branch remote hanya `main` + branch sesi ini — tidak ada pekerjaan paralel yang sedang
mengklaim nomor migrasi. Tapi ada **2 PR lama yang masih terbuka**:
[#18](https://github.com/MEAgrup/MEAGO_MSDPS/pull/18) (11 Agu, deteksi service-role key
project Supabase yang salah — bertetangga dengan temuan 3c) dan
[#14](https://github.com/MEAgrup/MEAGO_MSDPS/pull/14) (19 Jul, guard anti-campur MSDPS vs
MCN MEA).

---

## 6. TASK LIST untuk menyelesaikan build plan

🔒 = butuh keputusan user dulu. Urut dari yang paling siap dikerjakan.

### Siap dikerjakan sekarang — tanpa keputusan

| # | Pekerjaan | Kenapa | Ukuran |
|---|---|---|---|
| ~~**T1**~~ | ~~Migrasi `0353`: resmikan `auth_users_token_defaults()` + trigger `auth.users`~~ | ✅ **SELESAI 2026-09-05** — sudah diterapkan ke staging + production, nol perubahan struktur di keduanya. Bukti di §8 | — |
| ~~**T2**~~ | ~~Rekonsiliasi riwayat migrasi kedua environment~~ | ✅ **SELESAI 2026-09-05** — `0354`, seluruh 75 file repo kini tercatat di kedua environment. Bukti + temuan turunan (14 file repo sudah diedit sesudah diterapkan) di §9 | — |
| **T3** | **Tutup atau lanjutkan PR #14 & #18** | Dua PR menganggur >3 minggu; #18 menyentuh area yang sama dengan T1 | kecil |

### Butuh keputusan user

| # | Pekerjaan | Yang harus diputuskan |
|---|---|---|
| **T4** 🔒 | **Item 5 — model realisasi POI (A / B / C)** | Rekomendasi tetap **B** (`poin` jadi turunan `poi_sop_progress`). Konfirmasi dulu: (a) apakah "Actual VT" memang setara `kreator_realized`? (b) apa padanan `visit_checked` — langkah visit di `poi_sop_steps` selesai? Sesudah diputus, nasib `update_poi_realisasi` (temuan 3d) ikut selesai: diresmikan (opsi A) atau dibuang (opsi B). **Ini yang membuat papan skor BD menampilkan 0 untuk semua orang padahal pekerjaannya nyata** |
| **T5** 🔒 | **Item 2 — jalankan satu campaign Fase G end-to-end** | Butuh: (a) email kreator untuk minimal 1 akun portal uji (sekarang **1 dari 2.250**), (b) file export TikTok "Content Analysis › Video List" asli, (c) izin memakai data uji di production. Prosedurnya sudah siap di `docs/RUNBOOK_CAMPAIGN_PERTAMA.md`. Kerjakan **sesudah T1** |
| **T6** 🔒 | **Item 4 — drop `crm_leads` + `crm_transaksi`** | Konfirmasi eksplisit (tidak bisa dibatalkan). **Catatan baru: staging punya 5 + 1 baris** — pastikan itu data uji. Sesudah ini, selisih repo ↔ live tinggal temuan 3c/3d |
| **T7** 🔒 | **Item 6 — 70 deal "belum lengkap"** | Dilengkapi manual, import ulang, atau dibiarkan? Sudah ditandai di UI dan disaring dari Portal Kreator, jadi tidak mendesak — tapi 70 dari 81 deal berarti tracker POI hanya melihat 11 deal |

### Belum bisa dijadwalkan

| # | Pekerjaan | Blocker |
|---|---|---|
| **T8** | **Isi kolom segmentasi kreator** (`niche` 1/2.250, `city` 33, `creator_level` 32, `jenis_creator` 1) | Sampai terisi, campaign **wajib** membiarkan seluruh `eligible_*` NULL — filter apa pun akan meloloskan nyaris nol kreator. Keputusan user 2026-09-04: campaign pertama memang tanpa filter, pengisian diurus belakangan |
| **T9** | **Buka Portal Kreator bertahap** | Roster tidak membawa email. Akun dibuat satu per satu lewat UI `/meago/creators` (sudah diperbaiki agar tidak merender 2.250 form). Tidak ada impor massal — keputusan user |

### Urutan yang disarankan

~~`T1`~~ ✅ → ~~`T2`~~ ✅ → `T4` (keputusan) → `T5` (campaign pertama, uji nyata) → `T6` → `T7`.

T1 sudah selesai. Berikutnya yang memblokir adalah **T4** — keputusan model realisasi POI —
karena nasib `update_poi_realisasi` (satu dari lima objek sisa di §3) ikut ditentukan di
sana, dan papan skor BD tetap menampilkan 0 untuk semua orang sampai itu dibereskan.

---

## 7. Cara memeriksa ulang

Paritas dua environment — **bandingkan HASH, bukan JUMLAH**, dan **normalisasi definisi
fungsi dulu** (buang komentar `--` + whitespace), kalau tidak akan muncul 26 "fungsi
berbeda" palsu:

```bash
psql -h /tmp -p 55432 -U postgres -d msdps_reset -tAq \
  -f scripts/schema_fingerprint.sql | LC_ALL=C sort > /tmp/fp_local.txt
# jalankan isi file yang sama lewat MCP Supabase execute_sql di staging & production
LC_ALL=C diff /tmp/fp_staging.txt /tmp/fp_prod.txt
```

**Dan bandingkan juga ke database bersih hasil repo**, bukan hanya dua environment satu
sama lain. Kalau langkah itu dilewat, temuan §3 tidak akan pernah muncul.

---

## 8. T1 SELESAI — migrasi `0353_auth_users_token_defaults` (sore 2026-09-05)

`supabase/migrations/0353_auth_users_token_defaults.sql` meresmikan
`public.auth_users_token_defaults()` + trigger `auth.users.auth_users_token_defaults` ke
repo. Nomor `0353` diperiksa bebas lebih dulu di `git log`, branch remote, **dan** riwayat
migrasi kedua environment (jebakan §3 poin 1).

### Cara memastikan ia benar-benar no-op

Badan fungsi disalin **byte demi byte** dari `pg_get_functiondef` production — termasuk
spasi perataan `:=` — supaya bahkan hash MENTAH tidak berubah. Komentar penjelas ditaruh di
luar badan fungsi, tidak di dalamnya; komentar di dalam badan akan mengubah hash mentah dan
memunculkan "drift" palsu, kelas alarm palsu yang sempat melahirkan kesimpulan keliru di
item 3b.

Triggernya dijaga `if not exists` (bukan `create or replace trigger`) supaya benar-benar
tidak menyentuh apa pun di environment yang sudah punya.

Satu hal yang hampir terlewat: fungsi ini **SECURITY INVOKER**, jadi ia berjalan sebagai
peran yang menulis ke `auth.users` — `supabase_auth_admin`. Kalau peran itu tidak punya
EXECUTE, insert GoTrue justru gagal karena permission dan keadaannya **lebih buruk**
daripada bug yang mau ditambal. Grant-nya ikut diresmikan, dijaga pemeriksaan keberadaan
peran supaya `pg_test_reset.sh` (Postgres polos, tanpa peran Supabase) tetap lolos.

### Verifikasi

| Uji | Hasil |
|---|---|
| `bash scripts/pg_test_reset.sh` | ✅ **74 migrasi lolos dari nol** |
| hash fungsi di reset repo vs production (mentah) | `b0af0c96…` = `b0af0c96…` ✅ |
| hash fungsi (dinormalisasi) | `4e96ef3e…` = `4e96ef3e…` ✅ |
| hash trigger `auth.users` | `52fb7b51…` di reset, staging, production ✅ |
| idempotensi — file di-apply 3× ke DB bersih | tetap **1** trigger ✅ |
| staging sesudah apply | 12/12 kategori hash **tidak berubah**, ACL fungsi sama persis |
| production sesudah apply | 12/12 kategori hash **tidak berubah**, 106 fungsi, 147 trigger |
| paritas staging ↔ production sesudahnya | **12 dari 12 identik** (11 kategori lama + `auth_trigger` `cc481222…`) |

### Selisih repo ↔ live sekarang: 5 objek

| Objek | Status |
|---|---|
| `crm_leads` + `crm_leads_validate()` | menunggu keputusan T6 |
| `crm_transaksi` + `crm_transaksi_validate()` | menunggu keputusan T6 |
| `update_poi_realisasi(...)` | menunggu keputusan T4 |

Sesudah kelima objek itu dikecualikan, **103 fungsi di reset repo cocok persis dengan
production** (`48d8be56…` di kedua sisi), begitu pula kolom, constraint, index, policy, RLS,
trigger, view, enum, bucket, dan cron job. Tidak ada lagi objek live tak terdokumentasi yang
tidak punya jalur keputusan.

### ⚠ Jebakan baru yang ditemukan saat verifikasi ini — collation

Saat membandingkan, hash agregat fungsi sempat berbeda antara reset lokal dan production
(`5e4cfd83…` vs `48d8be56…`) padahal isinya identik. Penyebabnya **bukan drift**: database
reset lokal ber-collation `C.UTF-8`, Supabase `en_US.UTF-8`, jadi `order by name` di dalam
`string_agg` menghasilkan **urutan berbeda untuk isi yang sama**. Sesudah diurutkan
deterministik (`LC_ALL=C sort` di luar database), kedua sisi menghasilkan `48d8be56…`.

Konsekuensinya untuk cara kerja: **jangan meringkas sidik jari jadi satu hash agregat di
dalam SQL.** Pakai `scripts/schema_fingerprint.sql` sebagaimana dirancang — keluarkan satu
baris per objek, urutkan dengan `LC_ALL=C sort` **di luar** database, lalu `diff`. Prosedur
resmi di `SCHEMA_DRIFT.md` memang sudah begitu; jalan pintas agregat yang menyimpang darinya,
dan jalan pintas itu berbohong.

---

## 9. T2 SELESAI — migrasi `0354_reconcile_migration_ledger`

### Pemetaan nama dibuktikan lewat isi, bukan ditebak

Kolom `statements` di `supabase_migrations.schema_migrations` menyimpan SQL yang
benar-benar dijalankan. Setiap baris riwayat di-hash sesudah dinormalisasi (komentar `--`
+ whitespace dibuang) lalu dicocokkan ke hash file repo dengan normalisasi yang sama.
**28 dari 36 rename cocok hash persis** — bukti bahwa baris riwayat itu memang file repo
tersebut, hanya beda konvensi nama. Delapan sisanya berpadanan nama secara literal.

### ⚠ Temuan turunan: 14 file repo sudah DIEDIT sesudah diterapkan ke production

Hash yang tidak cocok padahal namanya berpadanan berarti file repo-nya berubah sesudah
dijalankan. Panjang SQL ternormalisasi, production vs file repo hari ini:

| Migrasi | di production | di repo | selisih |
|---|---:|---:|---:|
| `0004_phase0_status_machine` | 1.392 | 1.461 | +69 |
| `0007_phase0_okr` | 1.396 | 1.399 | +3 |
| `0008_phase0_hardening` | 852 | 900 | +48 |
| `0101_module1_leads` | 7.503 | 7.528 | +25 |
| `0208_module13_merchant_health` | 16.618 | 16.659 | +41 |
| `0316_mcn_creator_status_kontrak` | 215 | 109 | **−106** |
| `0317_gmv_video_weekly_tracking` | 2.971 | 2.969 | −2 |
| `0320_brand_deals_poi_reconcile` | 2.607 | 2.675 | +68 |
| `0339_schema_reconcile` | 1.149 | 1.213 | +64 |
| `0340_portal_deals_filter` | 589 | 633 | +44 |
| `0343_go_campaign_participants` | 9.063 | 9.249 | +186 |
| `0344_go_campaign_submissions` | 7.354 | 8.087 | **+733** |
| `0345_go_campaign_payouts` | 7.871 | 8.298 | **+427** |
| `0346_go_campaign_ingest` | 5.958 | 6.652 | **+694** |

Ini **bukan kondisi darurat**: skema hasil `db reset` sudah dibuktikan identik dengan
production (§3, §8), jadi suntingan itu tidak mengubah keadaan akhir — pola yang cocok
dengan penambahan penjaga idempotensi, dan untuk empat migrasi campaign dengan penulisan
ulang saat tabrakan nomor `0341`–`0343`. Tapi konsekuensinya nyata: **untuk 14 migrasi ini,
SQL yang dijalankan production BUKAN SQL yang ada di repo hari ini.** Jangan pernah
menyimpulkan "production menjalankan file X" hanya dari nama.

### Yang sengaja TIDAK dilakukan: tidak ada baris yang dihapus

Rencana T2 semula menyebut "bersihkan baris sampah di staging". Sesudah diperiksa, itu
keputusan yang salah. Baris seperti `0317_acquisition_extra_fields`,
`0319_acquisition_followups`, dan `tmp_vgmv_*` mencatat migrasi yang **benar-benar pernah
jalan** di staging; objeknya memang sudah dibuang, dan baris `0351` itulah yang mencatat
pembuangannya. Riwayat adalah catatan "apa yang pernah jalan", bukan "file apa yang ada
sekarang" — menghapusnya menghilangkan bukti.

Karena itu **kedua riwayat masih akan berbeda sesudah T2, dan itu benar**: staging punya
sejarah eksperimen yang production tidak punya.

### Hasil

| | production | staging |
|---|---:|---:|
| baris riwayat | 85 | 90 |
| **file repo yang belum tercatat** | **0 dari 75** | **0 dari 75** |
| baris tanpa file repo (riwayat jujur) | 10 | 14 |
| baris hasil back-fill | 2 | 0 |

Dua back-fill di production — `0312_creator_portal_f2` (versi `20260718080703`) dan
`0315_weekly_retention_dedup` (`20260723064224`) — dijaga dua lapis: hanya disisipkan bila
barisnya belum ada **dan** objek penandanya benar-benar ada (`creator_reports`,
`upload_batches.file_hash_full`). Jadi ini bukan riwayat karangan, dan `created_by`
menandainya sebagai back-fill supaya bisa dibedakan dari catatan organik.

Baris tanpa file repo di production: `0310_poi_dealing`, `0318_auth_users_token_null_guard`
(diresmikan ulang oleh `0353`), `0336_ops_name_add_fifas` (digantikan `0350`),
`crm_leads_transaksi` ×2 dan `crm_hardening_revoke_definer` (T6),
`phase0_0004b_status_transitions_rls`, `phase0_0009_function_grants_fix`,
`faseB_03_fix_status_enum_cast`, `faseB_08_normalize_phone_searchpath`.

**Verifikasi:** `pg_test_reset.sh` → **75 migrasi lolos dari nol**, dan di database bersih
daftar nama riwayat **identik persis** dengan daftar 75 file repo — bukti `0354` benar-benar
no-op di environment yang sudah benar.
